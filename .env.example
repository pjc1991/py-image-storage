# Image Storage Service Configuration

# Required: Source directory to monitor for images
UNCOMPRESSED=\\NAS\FILES\uncompressed

# Required: Destination directory for compressed images
COMPRESSED=\\NAS\FILES\compressed

# Optional: How often to run cleanup (in seconds)
CLEANUP_INTERVAL_SECONDS=60

# Optional: Minimum file size to compress (in KB)
MIN_FILE_SIZE_KB=1024

# Optional: Maximum resolution for images (pixels)
MAX_RESOLUTION=1920

# Optional: WebP compression quality (1-100)
COMPRESSION_QUALITY=90

# Optional: Cache settings
CACHE_MAXSIZE=100
CACHE_TTL=60

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
